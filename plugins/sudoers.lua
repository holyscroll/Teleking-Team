do

function run(msg, matches)
  return [[
#Sudoers!
  
  1.@holyscrolls🌐 #Developer ( use #adddeveloper command to add #holyscrolls  in your Groups & SuperGroups. )
  
  2.@mahssa🌐 #Manager ( use #addmanager command to add #mahssa in your Groups & SuperGroups. )
  
]]
end
return {
  description = " ", 
  usage = " ",
  patterns = {
    "^[#!/]sudoers$",

  },
  run = run
}
end
